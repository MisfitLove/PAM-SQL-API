{
  "name": "pam-sql-api",
  "version": "1.0.0",
  "description": "api for PAM project",
  "main": "api.js",
  "dependencies": {
    "mysql": "^2.17.1",
    "restify": "^8.3.3"
  },
  "engines": {
    "node": "10.13.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node api.js"
  }
}
